{
  "name": "carbon-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio carbon-test",
    "create-local": "graph create --node http://localhost:8020/ carbon-test",
    "remove-local": "graph remove --node http://localhost:8020/ carbon-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 carbon-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
